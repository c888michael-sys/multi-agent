{
  "name": "multi-agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Multi-agent LLM orchestration on free-tier APIs with key/account failover.",
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke": "tsx scripts/smoke.ts",
    "verify-keys": "tsx scripts/verify-keys.ts",
    "cli": "tsx src/cli.ts"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
